name: Container Build

on:
  release:
    types: [published]
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: cwaits6/.github/.github/workflows/container-build.yml@fix/remove-top-level-permissions
    with:
      dockerfile: Dockerfile
      platforms: linux/amd64,linux/arm64
