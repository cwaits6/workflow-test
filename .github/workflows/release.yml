name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: cwaits6/.github/.github/workflows/release.yml@fix/remove-top-level-permissions
    with:
      go-binary-name: workflow-test
      go-ldflags-prefix: main
